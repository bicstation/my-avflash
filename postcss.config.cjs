// postcss.config.js (この内容に置き換えてください)
// モジュールのエクスポート方法も重要です。
module.exports = {
  plugins: {
    // Tailwind CSSをPostCSSのプラグインとして登録
    tailwindcss: {}, 
    // ブラウザのベンダープレフィックスを自動で付与
    autoprefixer: {}, 
  },
};